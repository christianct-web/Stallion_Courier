# Python
backend/.venv/
.venv/
__pycache__/
*.pyc

# Runtime outputs
backend/data/generated/
backend/data/.locks/

# Local SQLite database + one-time JSON→DB migration markers (Phase 3B).
# Production uses Postgres via DATABASE_URL; the dev DB is disposable.
backend/data/stallion.db
backend/data/stallion.db-*
backend/data/.migrated/

# Node
frontend/node_modules/
frontend/dist/

# Logs / env
*.log
.env

# Editor / backup artifacts
*.backup
*.bak
*.before_*
