# F2: API traffic must go over HTTPS to the Caddy-fronted backend.
# NEVER point this at a raw IP:port — the previous value exposed the VPS
# address and sent customs data over plain HTTP.
# Replace api.stallion.example.com with your real API hostname.
/api/*  https://api.stallion.example.com/:splat  200
/*      /index.html                              200
